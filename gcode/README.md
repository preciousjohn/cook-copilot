# G-code
- calibration: Prefixed G-code files that users can use to calibrate the extrusion multiplier.
- config: Configuration files for the printer.
- example: Example code.
- template: The compiler agent should prepend `start.gcode` to the top of the G-code file and append `end.gcode` to the bottom.